/**
 * Curated connector directory — canonical copy lives in
 * pi-package/extensions/mcp/connector-catalog.ts so the CLI connectors
 * panel (shipped in the packaged tarball) shares the exact same list.
 */
export * from "../../../pi-package/extensions/mcp/connector-catalog";
